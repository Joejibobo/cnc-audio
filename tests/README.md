# CNC Audio — Tests

## Structure

```
tests/
├── engine/        # Unit + integration tests for the timeline generator
├── renderer/      # Tests for the FFmpeg rendering pipeline
├── analyzer/      # Tests for analysis accuracy and confidence scoring
└── fixtures/      # Shared test fixtures (short generated audio files, sample .cnc projects)
```

## Running Tests

*Setup instructions coming in Phase 3.*
